#include <iostream>
#include "shared.h"

using std::vector;

vector<TT> iteration(const vector< vector<TT>>& matrix, const vector<TT>& vector) {
	vector< vector<TT>> Ematrix;
	vector< vector<TT>> result;

	return result;
}

void getIterational() {
	vector<vector<TT>> matrix;
	vector<TT> rightVect;
	inputMatrix(matrix);
	inputVector(rightVect);
	iteration(matrix, rightVect);
}